namespace Chess_API.Enums;

/// <summary>
/// Represents the state where the white & black pieces are either on the
/// top or the bottom
/// </summary>
public enum PlayingDirection
{
    WhiteTop,
    WhiteBottom
}